[tool.poetry]
name = "psychologyservice"
version = "0.1.0"
description = ""
authors = ["ILya"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
fastapi = {extras = ["all"], version = "^0.115.0"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
